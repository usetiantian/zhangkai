# 水：实施与验证计划

> 每完成一步，在该项后追加 `✅`、完成时间、验证命令和证据路径。测试通过不等于阶段完成；必须满足阶段出口条件。

状态：`⬜ 未开始` · `🔄 进行中` · `⛔ 阻塞` · `✅ 已验证完成`

## Phase 0 — 独立项目与架构基线

目标：确定边界、原则、模块、数据契约和验收方式，不写未经验证的自治功能。

- ✅ **0.1 创建独立 ASCII 目录结构**  
  完成：2026-07-31。证据：`C:/Users/87999/shui/` 下已建立完整认知模块拓扑；未引用 Nexus 文件。验证：目录清单与 `ARCHITECTURE.md` v0.3 一致。
- ✅ **0.2 写项目定义和独立性原则**  
  完成：2026-07-31。证据：`README.md`。
- ✅ **0.3 写架构基线**  
  完成：2026-07-31。证据：`ARCHITECTURE.md`。
- ✅ **0.4 记录首个架构决策**  
  完成：2026-07-31。证据：`docs/decisions/ADR-0001-independent-stdlib-kernel.md`。
- ✅ **0.5 定义 v1 数据契约**  
  完成：2026-07-31。实现：`contracts/models.py`。测试：`tests/test_contracts.py`，7/7 通过。RED：缺少 `contracts.models`；GREEN：JSON 往返、不可变与非法输入验证全部通过。
- ✅ **0.6 建立项目自检命令**  
  完成：2026-07-31。命令：`python check.py`。验证：编译、目录拓扑、禁止第三方依赖和全量测试；10/10 通过。

出口：目录独立；无第三方运行依赖；数据契约测试全部通过；文档与实现一致。

## Phase 1 — 最小现实感知与证据链

目标：让水第一次可靠观察外部真实变化，不进行自治执行。

- ✅ 1.1 实现 Adapter 协议和能力 manifest  
  完成：2026-07-31。实现：`perception/base.py`、`capabilities/manifest.py`。
- ✅ 1.2 实现本地文件观察适配器  
  完成：2026-07-31。实现：`perception/file.py`；只读并注入时钟，不隐藏环境数据。
- ✅ 1.3 实现原始证据快照、时间和 SHA-256 哈希  
  完成：2026-07-31。实现：`provenance/store.py`；内容寻址快照及 SQLite 谱系。
- ✅ 1.4 实现幂等摄入和变化检测  
  完成：2026-07-31。相同来源和内容跨重启不重复；内容变化生成新观察版本。
- ✅ 1.5 用临时真实文件完成首次端到端验证  
  完成：2026-07-31。测试：`tests/test_perception_pipeline.py`，5/5 通过；全量 15/15 通过。测试发现并修复 Windows SQLite 连接未关闭问题。

验收：首次观察产生事件；相同内容不重复入库；变化产生新版本；证据可由哈希校验；重启后状态不丢失。

出口：不依赖模型，完成“外部变化 → 可追溯事件 → 持久世界状态”。

## Phase 2 — 世界模型与时间意识

- ✅ 2.1 区分观察、主张、事实、假设和未知项  
  完成：2026-07-31。新增 `Hypothesis`、`Unknown`、`Conflict` 契约，类型独立持久化。
- ✅ 2.2 实现来源谱系和独立来源分组  
  完成：2026-07-31。`WorldModel.register_source` 按发布者独立组计数，不把镜像转载当独立证据。
- ✅ 2.3 实现有效时间、观察时间和失效状态  
  完成：2026-07-31。查询时区分 `not_yet_valid`、`current`、`expired`。
- ✅ 2.4 实现冲突记录，不静默覆盖旧结论  
  完成：2026-07-31。相反主张与 Conflict 同时保留。
- ✅ 2.5 建立世界模型查询与快照测试  
  完成：2026-07-31。实现：`world_model/store.py`；专项 5/5、全量 20/20 通过；事实可追溯至 Claim 和 Evidence。

出口：任一结论都能回溯原始证据；冲突和过期知识可见。

## Phase 3 — 身份、价值权重和目标系统

- ✅ 3.1 定义持续身份与使命版本  
  完成：2026-07-31。`identity/store.py` 支持版本持久化与最新身份恢复。
- ✅ 3.2 定义显式价值权重来源和校准记录  
  完成：2026-07-31。无来源、无时区或越界权重均拒绝。
- ✅ 3.3 实现目标树、依赖和成功标准  
  完成：2026-07-31。目标依赖决定当前可执行集合。
- ✅ 3.4 实现候选目标竞争及评分解释  
  完成：2026-07-31。每个总分保留价值分量明细。
- ✅ 3.5 验证权重变化如何影响排序  
  完成：2026-07-31。安全与速度权重互换后排序按预期反转；专项 5/5、全量 25/25 通过。

出口：没有来源的权重不得伪装成已校准权重；目标选择可解释和复现。

## Phase 4 — 规划、执行和现实验证

- ✅ 4.1 定义 Plan、Step 和 Prediction 契约  
  完成：2026-07-31。Prediction 明确预期结果和验证条件；JSON 往返通过。
- ✅ 4.2 实现任务租约、幂等键和状态机  
  完成：2026-07-31。独占租约、幂等任务及过期重取均由 SQLite 保持。
- ✅ 4.3 实现首个只作用于 `shui/` 测试域的执行器  
  完成：2026-07-31。原子文件执行器拒绝越出声明根目录。
- ✅ 4.4 实现 ActionReceipt 和前后状态验证  
  完成：2026-07-31。执行回执记录前态及结果哈希，验证器读取现实文件确认。
- ✅ 4.5 实现失败恢复和中断续跑测试  
  完成：2026-07-31。支持回滚旧内容、删除新文件及租约过期恢复；专项 6/6、全量 31/31 通过。

出口：模型或进程声称完成不算完成，必须由验证器观测实际结果。

## Phase 5 — 自主水循环

- ✅ 5.1 从世界变化自主产生候选目标  
  完成：2026-07-31。新观察触发循环；未变化输入不重复行动。
- ✅ 5.2 生成多个候选计划并评价  
  完成：2026-07-31。候选目标及影响由外部显式传入，循环按价值权重输出分量和排序，不硬编码价值数据。
- ✅ 5.3 自主选择并执行可验证计划  
  完成：2026-07-31。生成 Plan、Step、Prediction 后原子执行报告并校验结果哈希。
- ✅ 5.4 根据结果更新世界模型与任务状态  
  完成：2026-07-31。证据先写世界模型，执行状态以现实验证结果确定。
- ✅ 5.5 无人工提醒连续完成一个完整循环  
  完成：2026-07-31。`cognition/loop.py` 完成观察→目标→评价→计划→预测→执行→验证；跨重启变化测试通过。专项 3/3、全量 34/34 通过。

出口：观察、目标、计划、行动、验证和记忆具有连续证据链。

## Phase 6 — 学习与经验形成

- ✅ 6.1 行动前保存预测  
  完成：2026-07-31。预测先入 SQLite，未存在预测的结果被拒绝。
- ✅ 6.2 比较预测与真实结果  
  完成：2026-07-31。`PredictionOutcome.matched` 显式记录预测偏差。
- ✅ 6.3 从重复事件提出候选经验  
  完成：2026-07-31。按策略聚合真实成功数；样本要求由调用者提供，无隐藏默认值。
- ✅ 6.4 用后续任务验证经验有效性  
  完成：2026-07-31。新增结果可改变最佳策略，经验不是永久结论。
- ✅ 6.5 证明同类任务指标实际改善  
  完成：2026-07-31。仅在候选和基线均满足样本要求时计算成功率差；专项 4/4、全量 38/38 通过。

出口：成长由跨任务数据证明，不由自述证明。

## Phase 7 — 自增能力

- ✅ 7.1 发现并记录能力缺口  
  完成：2026-07-31。缺失能力转为带输入、预期输出的 Gap 草案。
- ✅ 7.2 自动生成能力规格和验收测试草案  
  完成：2026-07-31。生成 CapabilitySpec、测试输入和预期结果，不凭语言描述晋级。
- ✅ 7.3 构建候选能力  
  完成：2026-07-31。候选版本独立保存，拒绝静态 import 且必须提供 execute 接口。
- ✅ 7.4 隔离测试并与基线对照  
  完成：2026-07-31。候选在 `python -I` 独立进程执行验收样本。说明：这是进程/环境隔离，不宣称为操作系统安全沙箱。
- ✅ 7.5 自动注册通过验证的新能力  
  完成：2026-07-31。失败候选不能注册；稳定注册表保存版本及当前指针。
- ✅ 7.6 使用新能力完成最初被阻塞的目标  
  完成：2026-07-31。新能力注册后完成原始转换任务；新版本可回滚至上一稳定版。专项 4/4、全量 42/42 通过。

出口：新能力有版本、manifest、真实测试、执行证据和回滚版本。

## Phase 8 — 开放世界持续学习

- ✅ 8.1 依据评估选择首个网络来源，不预设来源  
  完成：2026-07-31。选择与 Python 运行时直接相关的一手 CPython 官方仓库。首次 Releases API 返回真实 404 后未伪造成功，改用其实际 Tags API。
- ✅ 8.2 实现标准库 HTTP 只读适配器  
  完成：2026-07-31。`perception/web/adapter.py` 仅使用 urllib，真实 HTTPS 请求成功。
- ✅ 8.3 实现来源快照、缓存和条件请求  
  完成：2026-07-31。内容按 URL 缓存，ETag 二次请求返回未变化；真实响应 420 字节并保存 SHA-256。
- ✅ 8.4 实现变化、新颖度、相关性和时效分析  
  完成：2026-07-31。来源评价与注意力权重全部外部传入；测试中纠正了凭直觉选错来源的问题。
- ✅ 8.5 实现快循环与慢循环调度  
  完成：2026-07-31。周期由调用者显式配置，无隐藏轮询默认值。
- ✅ 8.6 连续运行并输出真实稳定性数据  
  完成：2026-07-31。本机真实 HTTP 连续试验 6 次：变化 1、未变化 5、失败 0；专项 6/6、全量 48/48 通过。另完成 CPython Tags API 首次获取与 ETag 条件复查。

出口：发现真实外部变化、保留来源、更新旧认知，并以现实数据验证稳定运行。

## Phase 9 — 扩展至获授权外部系统

每个系统单独完成：能力规格 → 授权方式 → 只读观察 → 沙箱执行 → 结果验证 → 恢复测试 → 稳定注册。

- ✅ 本机系统适配  
  完成：2026-07-31。标准库读取平台、Python 版本和真实磁盘容量；不伪造不可用指标。
- ✅ Git 仓库适配  
  完成：2026-07-31。可识别仓库、分支、HEAD、修改和未跟踪文件；非仓库明确返回 false。通过 SSH 将本地 main 安全推送到远程独立 `shui` 分支，未覆盖远程 main/master；远程 SHA 与本地 HEAD 完全一致。
- ✅ 外部 API 适配  
  完成：2026-07-31。真实 CPython GitHub Tags API 获取、缓存、哈希及 ETag 条件复查成功。
- ✅ 持续运行与故障演练  
  完成：2026-07-31。除既有恢复与 8 类故障矩阵外，使用 `config/soak.json` 对 CPython 官方 Tags API 执行配置门槛 6 轮：1 verified、5 unchanged、0 failed、世界记录 +3、审计事件 +6。随后生产 CLI 从同一状态恢复并完成 1 个来源、失败 0，审计链有效。证据：`docs/SOAK_REPORT.json`（首次暴露字节指标缺陷）与 `docs/SOAK_REPORT_2.json`（修正后的记录数指标）。该结论仅代表明确的 6 轮初始门禁，不宣称小时/天级稳定性。

不设拍脑袋日期，由前序阶段真实数据决定节奏。

## Phase 10 — 从验证内核到可长期运行系统

目标：把已验证但相对独立的模块接成可配置、可审计、可恢复、可持续运行的程序；不以测试集合代替真实运行系统。

- ✅ **10.1 全量实现与接线审计**  
  完成：2026-07-31。命令：`python -m audit.project`。证据：`docs/IMPLEMENTATION_AUDIT.md`。审计 39 个生产文件、13 个测试文件；确认无统一入口，3 个空模块，以及身份/注意力/学习/演化/审计和 HTTP 闭环断链。审计专项 2/2、全量 53/53 通过。
- ✅ **10.2 代码质量基线整理**  
  完成：2026-07-31。新增 `audit/quality.py` 和 `docs/QUALITY_BASELINE.md`。分批整理自主循环、能力演化、HTTP、接线审计、学习、执行、身份、注意力、Git、调度及剩余模块；确定性问题 131→0，全量测试 55/55 通过。质量扫描已接入 `python check.py`，后续出现压行或超长生产代码将直接导致自检失败。
- ✅ **10.3 严格配置系统**  
  完成：2026-07-31。实现 `config/loader.py`：身份、价值及来源、快慢周期、启用能力、状态/行动/审计路径和运行参数全部必填；未知字段、缺失字段、非正周期、非法权重及任意深度 token/secret/password/api_key 字段均拒绝。相对路径按配置文件目录解析。专项 5/5、全量 60/60 通过，质量问题保持 0。
- ✅ **10.4 追加式审计链**  
  完成：2026-07-31。实现 `audit/chain.py`：事件 ID、带时区时间、因果 ID、类型、结果和数据进入 JSONL；每条事件绑定前一哈希并 `fsync`。跨重启续链、重复 ID 拒绝、历史篡改检测通过；任意深度 token/secret/password/api_key/Authorization 字段拒绝。专项 5/5、全量 65/65 通过，质量问题保持 0。
- ✅ **10.5 统一启动与状态入口**  
  完成：2026-07-31。实现根目录 `shui.py`：`check|once|run|status` 均可调用；配置错误返回结构化错误和非零退出码。启动恢复或初始化持续身份、世界模型、任务库、审计链及演化能力注册表；状态输出身份、审计完整性/事件数、世界记录数、任务状态、配置能力和演化能力。`run --cycles N` 使用调用者显式次数，无次数则持续运行。专项 4/4、全量 69/69 通过，质量问题 0。
- ✅ **10.6 网络感知接入自主循环**  
  完成：2026-07-31。实现 `cognition/network_loop.py`，HTTP Observation 转为标准 Capture 并进入统一认知处理。200 变化形成 Observation→Evidence→Goal→Plan→Prediction→ActionReceipt→Verification，并按因果顺序写入 6 条哈希审计事件；Evidence、ActionReceipt、VerificationRecord 持久化到世界模型。ETag 304 及跨重启相同内容均不重复行动或审计。HTTP 超时由配置参数传入，不再硬编码。专项 2/2，相关回归 11/11，全量 71/71 通过，质量问题 0。
- ✅ **10.7 学习接入自主循环**  
  完成：2026-07-31。实现 `cognition/runtime.py`：严格配置新增候选 goals（描述、依赖、价值影响），运行协调器从 ShuiConfig 构造 ValueSet、GoalCandidate、NetworkLoop 和 LearningStore。`before_action` 在文件执行前持久化预测及策略，`after_action` 仅在现实验证后写 success/failure。样本不足沿用价值排序；达到配置 `learning_min_samples` 后优先历史成功率最高策略，后续证据仍可改变选择。专项 2/2，配置/CLI 回归 9/9，全量 73/73 通过，质量问题 0。
- ✅ **10.8 心跳、检查点与恢复**  
  完成：2026-07-31。实现 `recovery/coordinator.py`：每来源处理前更新心跳、完成后原子 `fsync+replace` 检查点；心跳记录时间、running/idle、循环数和最后来源。普通来源异常写 `source.failed` 与 `cycle.recovered` 审计并继续后续来源；进程级 KeyboardInterrupt 不吞掉，重启从最后确认索引恢复，完成整轮后索引归零并增加循环数。专项 3/3、全量 76/76 通过，质量问题 0。
- ✅ **10.9 故障注入矩阵**  
  完成：2026-07-31。实现 `experiments/fault_matrix.py`，统一记录场景名、状态、是否被限制、是否恢复和实际异常类型。真实覆盖 HTTP timeout、HTTP error、SQLite 独占锁、损坏 JSON 状态、执行 TimeoutExpired、`os.replace` 磁盘写失败、真实 git 客户端非法远程失败、候选能力验收失败共 8 类；每项恢复探针均成功。预期异常未发生会标记 `unexpected_success`，意外异常标记 `unexpected_failure`，不隐藏。专项 2/2、全量 78/78 通过，质量问题 0。
- ✅ **10.10 真实持续运行门禁**  
  完成：2026-07-31。实现 `experiments/soak.py` 和配置必填 `runtime.soak_cycles`；报告开始/结束时间、实际时长、门槛/尝试数、verified/unchanged/failed/recovered、世界文件及记录增长、审计增长和磁盘变化。首次真实 6 轮报告为 1 verified、5 unchanged、0 failed，但 `world_bytes_growth=0` 暴露 SQLite 预分配不能表示知识增长；保留首份报告并新增 `world_records_growth`，在独立状态重跑得到世界记录 +3、审计 +6。统一 CLI 后续真实恢复运行完成 1 来源、失败 0、审计有效。专项 2/2、全量 80/80 通过，质量问题 0。

Phase 10 出口：水可以由一个入口启动，在真实来源上持续运行，所有关键决定可追溯，失败可恢复，学习确实进入下一次决策，并由真实运行数据而非自述证明稳定。

## Phase 11 — 成熟化、认知接口与可替换智能

目标：从“可持续运行的单实例内核”升级为可由真实进程验证、支持并发审计、多来源感知、可替换模型、强化能力隔离并能查询自身世界模型的成熟系统。

- ✅ **11.1 Phase 10 后接线与风险审计**  
  完成：2026-07-31。证据：`docs/PHASE11_AUDIT.md`。重新审计 51 个生产文件、22 个测试文件，统一入口存在，仅 `feedback/` 空。确认 9 项风险：审计并发竞态、无子进程 CLI 测试、仅 HTTP 协议、无模型协议、能力继承环境权限、无查询 CLI、能力超时未接配置、仅 6 轮认证及反馈断链；形成风险顺序。全量 80/80 通过，质量问题 0。
- ✅ **11.2 CLI 真实进程级端到端测试**  
  完成：2026-07-31。新增 `tests/test_cli_subprocess.py`，用真实 `subprocess` 与本机 HTTP 服务验证 once、run、status、坏配置及无限 run 终止后重启；检查真实退出码/JSON、世界记录 3、有效审计、检查点和心跳文件。首次测试发现未关闭 PIPE ResourceWarning，改用 DEVNULL 后消除。`check` 由外层真实命令验证，避免测试发现递归。专项 3/3、全量 83/83 通过，质量问题 0。
- ✅ **11.3 多进程安全审计链**  
  完成：2026-07-31。实现 `audit/lock.py` 跨平台 OS 文件锁（Windows msvcrt/Posix flock），提供 `lock_timeout_seconds` 与 `lock_poll_seconds`。`AuditChain.append` 在可选锁存在时包裹；测试中 4 个并发子进程各写 5 条事件，共 20 条无丢失且全链验证通过；进程崩溃后 OS 自动释放锁。严格配置新增 `runtime.audit_lock_timeout_seconds` 与 `audit_lock_poll_seconds`；`NetworkLoop` 与 `LearningRuntime.from_config` 已接入。专项 2/2、全量 85/85 通过，质量问题 0。
- ✅ **11.4 多来源一手感知组合**  
  完成：2026-07-31。新增 `perception/rss/adapter.py`（RSS 2.0、ETag 缓存、新增项检测）和 `perception/sources.py` 协议注册表：GenericHttp、GitHubTags、RssSource，每来源独立 ID、独立缓存子目录。`build_source` 根据 `protocol` 字段分发。严格配置 `sources[*].protocol` 必填且只接受 `http / github_api / rss`；多源失败隔离由 `RecoveryCoordinator` 隔离。论文元数据协议在“至少三类”要求下保持开放，arXiv 适配器不抢先实现，避免拍脑袋。专项 5/5、全量 90/90 通过，质量问题 0。
- ✅ **11.5 可替换模型适配器**  
  完成：2026-07-31。实现 `models/protocol.py`（不可变 ModelRequest/ModelResponse、ModelError、Protocol）和 `models/builtin.py`（NullModelAdapter、CommandModelAdapter、OpenAICompatibleAdapter）。命令适配器支持 `{prompt}` 占位替换，OpenAI 适配器强制 Bearer token、缺失即抛错；Null 永不失败，可作安全默认。专项 5/5、全量 95/95 通过，质量问题 0。
- ✅ **11.6 强化候选能力隔离**  
  完成：2026-07-31。`CapabilityEvolution` 新增 `working_dir` 与 `environment` 白名单；`_run` 强制子进程 `cwd=working_dir`、`env=白名单+显式剥离 SHUI_SECRET_/OPENAI_API_KEY/ANTHROPIC_API_KEY/GITHUB_TOKEN`；候选只接受 `python -I` 隔离进程。测试 1：父进程 `SHUI_SECRET_TOKEN` 不能渗透；测试 2：声明 working_dir 下文件可被读取；测试 3：能力输出敏感键仍被评估器拒绝。仍受限：未做 OS 级 syscall 沙箱，仍依赖 Python 解释器隔离；未做 I/O 字节门槛；运行时长硬编码 10 秒（将由 11.6 后续阶段接管）。专项 3/3、全量 98/98 通过，质量问题 0。
- ✅ **11.7 世界模型查询与认知界面**  
  完成：2026-07-31。`WorldModel` 新增 `query`（类型、来源、全文、时间有效性）与 `explain`（证据链、冲突 ID、当前/过期状态）；`query` 跨记录反向追溯 source 引用。统一 CLI 新增 `query`、`trace`、`explain` 三个子命令，输出稳定 JSON。专项 2/2、全量 100/100 通过，质量问题 0。
- ✅ **11.8 分级真实运行认证**  
  完成：2026-07-31。实现 `experiments/certification.py` 等级评估器和 `CertificationConfig` 严格配置。`cycles` 档位基于已完成的 runtime 循环数颁发证书并写证据；`hours` 与 `days` 档位基于实际挂钟秒数，未达到不颁发。统一 CLI 新增 `cert` 子命令，按审计链首事件计算实际秒数并按循环事件统计实际循环。专项 5/5（其中真实子进程 2/2 验证 hours 拒绝 / cycles 颁发），全量 105/105 通过，质量问题 0。Phase 11 严格结论：cycles 等级证书已真实颁发；hours 与 days 等级保持未认证状态。

Phase 11 出口：真实子进程可操作水；并发写审计安全；多类一手来源可配置；模型可替换且不可用不拖垮内核；候选能力有更强隔离；人和水都能查询、追溯和解释世界模型；运行认证严格对应真实时间。

## 当前下一步

**Phase 11 已完成。** 后续工作：继续累积真实运行时间使 hours/days 认证达成；在保持零第三方依赖前提下评估 OS 级 syscall 沙箱与 I/O 字节门槛；启动外部论文元数据等第三类来源协议；引入更多模型适配器并接入认知决策。
